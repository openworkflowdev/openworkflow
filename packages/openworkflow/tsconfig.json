{
  "extends": ["../../tsconfig.base.json"],
  "compilerOptions": {
    "outDir": "dist"
  },
  "include": ["**/*.ts"],
  "exclude": [
    "dist",
    "**/*.test.ts" // see note in ./tsconfig.test.json
  ]
}
